const names = ['osvaldo alves', 'João Vitor', 'LUCAS VINICIUS', 'DaniELLe VeríSSImo'];

const upper = names.map((item) => item.toUpperCase());
const low = names.map((item) => item.toLowerCase());

console.log(upper);
console.log(low);