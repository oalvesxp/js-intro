const rate01 = 8.2;
const rate02 = 7.5;
const rate03 = 4.8;
const rate04 = Number.parseInt('6');

const avg = (rate01 + rate02 + rate03 + rate04) / 4; 

console.log('A média de notas é: ' + avg.toFixed(2));